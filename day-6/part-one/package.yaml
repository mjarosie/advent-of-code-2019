name:                part-one
version:             0.1.0.0
license:             BSD3
author:              "mjarosie"

extra-source-files:
- README.md

dependencies:
- base >= 4.7 && < 5
- split == 0.2.3.3
- containers == 0.6.0.1

executables:
  count-orbits:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
